name: Hello
on: push
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "Hello, world!"
        git log -1
        gree -a .
    - uses: actions/checkout@v4
      with:
        ref: main